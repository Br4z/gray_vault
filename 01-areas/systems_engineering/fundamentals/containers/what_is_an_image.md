---
reviewed_on: 2026-06-18
---

# What is an image?

It is a read-only template used to create containers. It includes the application code, runtime, libraries, dependencies and configuration needed for the application to run.

> A container image is not the running application itself. The image becomes a container when it is executed. In this sense, the image is the blueprint and the container is the running instance created from that blueprint.
