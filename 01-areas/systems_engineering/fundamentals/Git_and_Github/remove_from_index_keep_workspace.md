---
reviewed_on: "2026-04-08"
---

# Remove form index keeping workspace

```bash
git rm --cached <file>
```

> To discard all, use the wildcard `*`.

> Without the `--cached` would also remove the file from the workspace.
